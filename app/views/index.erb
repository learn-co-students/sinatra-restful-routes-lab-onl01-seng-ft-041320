<!DOCTYPE html>
<html>
  <body>
    <h1>Recipes Index</h1>
    <% @recipes.each do |recipe| %>
    <% recipe_link = "/recipes/#{recipe.id}" %>
    <p><a href="<%= recipe_link %>"><%= recipe.name %></a></p>
    <% end %>
  </body>
</html>
